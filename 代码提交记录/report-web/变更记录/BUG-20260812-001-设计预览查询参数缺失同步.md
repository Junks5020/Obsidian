---
tags:
  - report-web
  - bug
  - 提交追踪
status: reverted-on-6.5.2
date: 2026-08-12
updated: 2026-08-12
---

# BUG-20260812-001 设计预览查询参数缺失同步

来源提交：`ea10b9d5504441bbe2ba0c2411c363a3171d01cb`。

6.5.2 已覆盖设计态默认查询参数和 `operator` 处理（`85facdfb`），并回移默认值运行态处理（`2e5a2429`）。针对接口失败时仍缺失参数的问题，新增设计配置降级加载（`b1557caf`）。查询条件显隐相关变更不在本次同步范围内。

## 目标提交

原目标提交：`b1557caf` `fix: 补充设计预览查询配置降级`。

按用户要求，6.5.2 暂不合入查询设置，已由 `454519a9` 和 `b6f08cb4` 回退相关内容。

## 验证

`git diff --check` 通过。受依赖安装源限制，当前环境无法启动 Jest：`update-browserslist-db@^1.3.0` 无可用版本。
