---
tags:
  - report-web
  - PrintManager
  - PrintSetV2
  - i18n
  - 多语言
  - 缺失清单
status: active
date: 2026-09-14
created: 2026-09-14
updated: 2026-09-14
busType: PrintSetV2
missing_key_count: 40
invalid_value_count: 3
---

# PrintSetV2 多语言缺失清单（2026-09-14）

相关：[[00-多语言总览]]、[[PrintManager-中文清单]]

## 一、审计范围

- 业务类型：`PrintSetV2`
- 对应模块：`src/pages/PrintManager/`
- 页面范围：打印模板管理列表页、设计页、预览页及其引用的共享报表组件
- 审计日期：**2026-09-14**
- 对照来源：后端 `PrintSetV2` 多语言返回值，以及 `C:\Users\jinxu\Downloads\遗漏多语言-补充.md` 中的 `PrintSetV2` 补充段

## 二、结论

1. 列表页表头、搜索框、抽屉表单和操作弹窗使用的 key 已完成匹配，当前没有遗留的词包缺失项。
2. 仍有 **40 个 key** 未出现在当前后端返回值中，主要集中在设计页工具栏、右侧属性面板、表格右键菜单、Sheet 页签，以及预览页筛选和链接弹窗。
3. `遗漏多语言-补充.md` 的 `PrintSetV2` 段共有 382 个 key，但与下列 40 个缺失 key的精确匹配数量为 **0**。
4. 补充文件提供了正确的 `Layer`、`freezeToRowCol`，并使用 `floatingImageLabel` 作为浮动图片标签 key，可用于替代异常的 `K`。

## 三、按页面区域划分的缺失 key

### 1. 打印模板管理列表页

当前没有遗留的缺失 key。

### 2. 打印模板设计页

| 页面区域 | 缺失 key | 中文含义 |
| --- | --- | --- |
| 顶部设计工具栏 | `undo` | 撤销 |
| 顶部设计工具栏 | `redo` | 恢复 |
| 顶部设计工具栏 | `formatPainter` | 格式刷 |
| 顶部设计工具栏、表格右键菜单 | `border` | 边框 |
| 顶部设计工具栏、冻结面板 | `freeze` | 冻结 |
| 顶部设计工具栏、浮动图片面板 | `floatingImage` | 浮动图片 |
| 顶部设计工具栏、报表属性弹窗 | `reportProperty` | 报表属性 |
| 冻结面板 | `freezeToRow` | 冻结至第 N 行 |
| 冻结面板 | `unfreeze` | 取消冻结 |
| 表格右键菜单 | `insertRowAbove` | 在上方插入 |
| 表格右键菜单 | `insertRowBelow` | 在下方插入 |
| 表格右键菜单 | `insertColumnLeft` | 在左侧插入 |
| 表格右键菜单 | `insertColumnRight` | 在右侧插入 |
| 报表属性弹窗 | `globalProperty` | 全局属性 |
| 报表属性弹窗 | `currentWorksheetProperty` | 当前工作表属性 |
| Sheet 页签 | `sheetLimitReached` | 已达上限 |
| Sheet 页签 | `worksheetNameRequired` | 名称不能为空 |
| Sheet 页签 | `worksheetNameDuplicated` | 名称重复 |
| 单元格类型、图片属性、浮动图片面板 | `image` | 图片 |
| 单元格类型、表达式属性 | `expression` | 表达式 |
| 字段单元格、图表字段配置 | `aggregateMode` | 聚合方式 |
| 字段单元格、图表字段配置 | `aggregationMax` | 最大值 |
| 字段单元格、图表字段配置 | `aggregationMin` | 最小值 |
| 图表字段配置 | `aggregationSum` | 求和 |
| 图表字段配置 | `rename` | 重命名 |
| 图表样式、数据集标题设置 | `chartShowTitle` | 显示标题 |
| 图表样式 | `chartPrefix` | 前缀 |
| 图表样式 | `chartSuffix` | 后缀 |
| 图表样式 | `chartUnit` | 单位 |
| 图片属性、浮动图片面板 | `imageSource` | 图片来源 |
| 单元格及浮动图片数据过滤 | `dataFilter` | 数据过滤 |
| 图片、链接、树形、标题帮助弹窗 | `prompt` | 提示 |
| 链接设置 | `linkTarget` | 链接目标 |
| 链接设置 | `linkOpenMode` | 打开方式 |
| 图表、条件配置等输入框 | `inputPlaceholder` | 请输入 |
| 图表、条件配置等输入框兼容 key | `pleaseEnter` | 请输入 |

### 3. 打印模板预览页

| 页面区域 | 缺失 key | 中文含义 |
| --- | --- | --- |
| 图片双击预览（设计页与预览页共用） | `ImageUnavailable` | 图片暂不可预览 |
| 图片双击预览（设计页与预览页共用） | `ResetImagePreview` | 重置 |
| 数据筛选弹窗 | `inputPlaceholder` | 请输入 |
| 数据筛选弹窗兼容 key | `pleaseEnter` | 请输入 |
| 数据筛选弹窗 | `selectAll` | 全选 |
| 链接弹窗 | `linkTarget` | 链接目标 |
| 链接弹窗 | `linkAddressRequired` | 链接地址不能为空 |

> `inputPlaceholder` 与 `pleaseEnter` 是同一输入提示的兼容链。后端统一提供其中一个即可，但前后端应约定唯一首选 key。

## 四、后端待补充 key（去重，共 40 个）

```text
ImageUnavailable
ResetImagePreview
aggregateMode
aggregationMax
aggregationMin
aggregationSum
border
chartPrefix
chartShowTitle
chartSuffix
chartUnit
dataFilter
expression
floatingImage
formatPainter
freeze
freezeToRow
globalProperty
image
imageSource
inputPlaceholder
insertColumnLeft
insertColumnRight
insertRowAbove
insertRowBelow
linkAddressRequired
linkOpenMode
linkTarget
pleaseEnter
prompt
redo
rename
reportProperty
selectAll
sheetLimitReached
undo
unfreeze
worksheetNameDuplicated
worksheetNameRequired
currentWorksheetProperty
```

## 五、补充文件核对结果

`遗漏多语言-补充.md` 中 `PrintSetV2` 段共有 382 个 key：

- 与上述 40 个缺失 key 精确匹配：**0 个**
- 直接合入该补充段后，上述 40 个 key 仍需另外补充
- 文件中存在下列近似 key，但不能直接视为同一个词条：

| 缺失 key | 补充文件近似 key | 说明 |
| --- | --- | --- |
| `border` | `AllBorder`、`ClearBorder` | 分别表示全边框、清除边框，不等于通用“边框” |
| `dataFilter` | `filter` | “数据过滤”与“筛选”语义范围不同 |
| `floatingImage` | `FloatingImageLayer` | 后者表示“浮动图片图层” |
| `reportProperty` | `Property` | 后者仅表示通用“属性” |
| `prompt` | `titleTip` | 后者只适用于标题提示 |
| `K` | `floatingImageLabel` | 建议统一使用语义明确的 `floatingImageLabel` |

## 六、异常返回值

| key | 当前后端返回值 | 问题 | 建议值/处理 |
| --- | --- | --- | --- |
| `K` | `{{image}} {{index}}_ng` | key 无语义，且与补充文件命名不一致 | 停用 `K`，统一使用 `floatingImageLabel: '{{image}} {{index}}'` |
| `Layer` | `{{image}}・{{image}}・ 图层{{}}{{image}}_ng` | 包含重复及空占位符 | 使用补充文件中的 `Layer: '图层'`；当前代码优先使用 `Layer2` 规避 |
| `freezeToRowCol` | `冻结至{{frozenHeader}}{{frozenHeader}}_ng` | 同一占位符重复，无法区分行列 | 使用补充文件中的 `冻结至第{{row}}行第{{col}}列` |

## 七、建议处理顺序

1. 后端先修复 `Layer`、`freezeToRowCol`，并将 `K` 统一为 `floatingImageLabel`。
2. 按第三节页面区域补齐 40 个缺失 key。
3. 前后端统一大小写及命名规则，优先使用小驼峰语义 key。
4. 补词后重新验证设计页首次加载、右侧属性弹窗、右键菜单、Sheet 页签、图片预览和预览筛选弹窗。

## 八、日期备注与验证边界

- **2026-09-14**：完成 `PrintSetV2` 后端返回值、前端调用路径及补充文件的静态比对。
- **2026-09-14**：专项自动化回归与既有 TableManager 多语言回归合计 13 项通过。
- **2026-09-14**：真实浏览器验证受本机 CUA 认证方式限制，错误为 `unsupported Codex auth method: apikey`，待环境恢复后补充页面实测结果。
