---
tags:
  - version-management
  - sync-audit
  - report-web
  - ng-design
---

# 6.5.2-dev、6.5.2 与 master 同步审计

## 审计边界

- 代码仓库：`C:\Users\jinxu\workspace\xzd\report-web`
- 组件库：`C:\Users\jinxu\workspace\xzd\ng-design`，分支 `ljx-7.0`
- 作者筛选：仅 `liujinxu <liujinxu@newgrand.com>` 的 `6.5.2-dev` 来源提交
- `6.5.2-dev` 与 `6.5.2`：先使用 `git cherry`，再逐文件检查来源提交的有效新增代码行是否存在于目标分支。
- `master`：不以 report-web 的 commit hash 直接判断。`45295d51` 已将报表组件抽离，master 的 `package-lock.json` 锁定 `@newgrand/udp-report-table` 为 `7.0.6`，因此需要结合 ng-design 功能提交判断。

## 6.5.2-dev 至 6.5.2

近期 22 条 `fix:` 记录中，5 条存在严格补丁等价提交，4 条新增代码完整匹配，9 条部分匹配，4 条未发现代码证据。

| 6.5.2-dev 提交 | 修复摘要 | 6.5.2 状态 | 6.5.2 对应提交/证据 |
| --- | --- | --- | --- |
| `2b372473` | 预览与打印 PDF 居中样式不一致 | ✅ 已同步 | `a620115c`，严格补丁等价 |
| `e26ca4d4` | 强制分页合并后 PDF 导出 | ❌ 待同步 | 有效新增代码 `0/4` 匹配 |
| `1941c028` | 报表保存后停留当前页 | ✅ 已同步 | `3c1444b0`，严格补丁等价 |
| `75155fcf` | 增加关联查询接口 | ⚠️ 验证中 | 代码层匹配 `7/14` 条有效新增行 |
| `a330ee7b` | 关联查询传参 | ⚠️ 验证中 | 代码层匹配 `2/17` 条有效新增行 |
| `7002f3a5` | 异常行高 | ⚠️ 验证中 | 代码层匹配 `1/5` 条有效新增行 |
| `d6334caf` | 跳转缺少字段类型参数 | ⚠️ 验证中 | 代码层匹配 `14/24` 条有效新增行 |
| `fa8d500b` | 跳转不携带字段类型参数 | ⚠️ 验证中 | 代码层匹配 `66/90` 条有效新增行 |
| `efcba723` | 链接跳转不携带参数 | ✅ 已同步 | 代码层完整匹配 `156/156` 条有效新增行 |
| `d8fbd367` | 打印设置默认查询及表单展示 | ✅ 已同步 | `53660fcd`，严格补丁等价 |
| `e0dfdf9c` | 树状预览展开折叠 | ✅ 已同步 | 代码层完整匹配 `325/325` 条有效新增行 |
| `bcb09f59` | 树形级次从 1 开始显示 | ✅ 已同步 | `ac40c7a3`，严格补丁等价 |
| `8fffc064` | 增加 Git 忽略文件 | ❌ 待同步 | 未发现目标提交 |
| `80f9ad8f` | 增加浮动图片功能 | ⚠️ 验证中 | 代码层匹配 `53/192` 条有效新增行 |
| `03fd80b6` | 树形层级 1 收起子节点 | ✅ 已同步 | 代码层完整匹配 `26/26` 条有效新增行 |
| `05398cd5` | PDF 定位问题 | ⚠️ 验证中 | 代码层匹配 `2/3` 条有效新增行 |
| `63ebca8c` | 浮动图片功能补充 | ⚠️ 验证中 | 代码层匹配 `8/97` 条有效新增行 |
| `58a68087` | 树形报表默认展示层级 | ✅ 已同步 | 代码层完整匹配 `107/107` 条有效新增行 |
| `2e3b8abe` | 打印设计页面换行 | ❌ 待同步 | 有效新增代码 `0/2` 匹配 |
| `df2a4451` | 隐藏行和列 | ⚠️ 验证中 | 代码层匹配 `9/88` 条有效新增行 |
| `772c74d5` | 浮动图片列使用字母格式 | ❌ 待同步 | 有效新增代码 `0/0`，需功能验证 |
| `19b4f8e5` | 管理预览重复发送接口 | ✅ 已同步 | `0adf7185`，严格补丁等价 |

## master 组件库映射

`master` 的 `45295d51` 在 2026-07-21 将 report-web 内的报表设计、预览、打印和树形渲染等组件移除，改为依赖 `@newgrand/udp-report-table`。随后 `7a059be1` 取消本地 link 包，`package-lock.json` 锁定组件包版本为 `7.0.6`。

| 功能范围 | 来源修复 | master / ng-design 证据 | master 状态 |
| --- | --- | --- | --- |
| 预览与打印居中样式 | `2b372473` | master 存在严格等价提交 `99e0126b` | ✅ 已同步 |
| 报表设计、预览、打印、链接和树形基础能力 | 多项历史修复 | report-web `45295d51` 抽离；ng-design `f073de7b` 创建 `udp-report-table` 并迁入 133 个文件 | ✅ 组件已迁移 |
| 隐藏行和列 | `df2a4451` | ng-design `294c3ebe` 的 `udp-report-table 7.0.6` 包含 `src/shared/hiddenCells.ts`；master 锁定同版本 | ✅ 已同步（组件库代码） |
| 树形级次、树形展开和默认层级 | `e0dfdf9c`、`bcb09f59`、`03fd80b6`、`58a68087` | ng-design `5ea66684` 同步树层级功能；`7.0.6` 含 `src/preview/utils/treeShowLevel.ts` | ✅ 已同步（组件库代码） |
| 浮动图片 | `80f9ad8f`、`63ebca8c` | ng-design `5ea66684` 同步浮动图片功能；`7.0.6` 含 `src/floatingImage/index.tsx` | ✅ 已同步（组件库代码） |
| 强制分页、打印设置、PDF 定位和打印换行 | `e26ca4d4`、`d8fbd367`、`05398cd5`、`2e3b8abe` | 代码已进入组件库迁移范围，但未找到与 dev 提交严格等价的 master 或 ng-design 提交 | ⚠️ 验证中 |
| 跳转参数与关联查询 | `75155fcf`、`a330ee7b`、`d6334caf`、`fa8d500b`、`efcba723` | 链接基础组件已迁移，但接口和参数处理仍可能属于 report-web 应用层 | ⚠️ 验证中 |
| 保存后停留当前页、异常行高、重复接口 | `1941c028`、`7002f3a5`、`19b4f8e5` | 未发现 master 的严格等价提交；其中重复接口为页面调用逻辑，不应仅凭组件迁移判定 | ❌ 待同步 |
| 浮动图片列字母格式 | `772c74d5` | 提交日期晚于 master 锁定的 `udp-report-table 7.0.6` 证据，未找到组件库对应提交 | ❌ 待同步 |

## 结论与后续核验

- `6.5.2`：优先处理所有 ❌ 待同步项；⚠️ 项需比较目标 commit 的实际 diff 后更新状态。
- `master`：组件迁移后的功能应在 `ng-design` 的 `@newgrand/udp-report-table 7.0.6` 中验证，不应在 report-web 中寻找被删除的原组件文件。
- 验证 master 时，重点执行隐藏行列、树形层级/展开、浮动图片、强制分页打印和链接参数的端到端回归。
