---
tags:
  - report-table
  - work-item
  - refactor
status: resolved
date: 2026-08-11
---

# 02 — 让树报表折叠由统一报表树拓扑驱动

Parent: [[work-items/report-table-functional-sync-refactor/spec]]

**What to build:** 引入可重建、不可变的报表树拓扑，并让树报表的初始化、初始层级、展开和收起完整地由这一事实来源驱动。筛选范围暂时保留旧解释，使本切片可独立落地。

**Blocked by:** [[01-冻结双基线与行为护栏]]

**Status:** resolved

- [x] 统一合并配置树元数据与响应 tree cells，并生成标准化节点、根节点和父子索引。
- [x] 拓扑模块是纯计算，不持有 Handsontable、store、cell meta 或其他 UI 状态。
- [x] 不增加全局缓存或新的 `WeakMap`；每次 sheet 数据加载可重建新快照。
- [x] 树初始化、`showLevel`、全部展开、全部收起和单节点切换复用统一拓扑。
- [x] 显式/推断父节点、重复节点、无效数字和断裂树保持既有兼容行为。
- [x] 树折叠隐藏与筛选隐藏继续组合生效。
- [x] 现有树调用 seam 与纯拓扑 seam 的测试保持绿色。
- [x] 不新增公开导出或改变组件契约。

## Comments

- 2026-08-11：已领取；按既定纯 `createTreeTopology` 契约与现有树初始化/交互 seam 开始测试驱动实施。
- 2026-08-11：新增包内不可变 `TreeTopology`，显式归一化配置树与响应 tree cells，仅保留树事实；覆盖来源优先级、显式/推断父节点、重复/无效/断裂/循环元数据、`showLevel`、隐藏行与输入隔离。
- 2026-08-11：`treeRender/utils.ts` 已迁移为 Handsontable/visibility adapter；初始化、全部展开、全部收起和单节点切换均复用拓扑快照，现有 `WeakMap` 只保留每个 hot 的交互状态。
- 2026-08-11：两轴代码审查以 `9438feecda9389315d3767629cb5f834c208ccf5` 为固定点；修正响应 cell UI 元数据泄漏、浅不可变、`showLevel` 重复归一化和节点 key 类型收窄后，Standards 与 Spec 最终均为 0 findings。
- 2026-08-11：完整四文件回归 60/60 通过；`npx tsc -p packages/@newgrand/udp-report-table/tsconfig.check.json --noEmit`、`npx lerna run build --scope @newgrand/udp-report-table`、Prettier 与 `git diff --check` 通过。
- 2026-08-11：复核固定 SHA 后的 `54f90c9f1ac9499324dd8635e619278a6ae0a17e`，该提交仅调整树缩进 CSS，已在 ticket 01 登记，不构成本 ticket 新发现的拓扑/折叠同步缺口。
- 2026-08-11：已提交 `f7450b96839b24317eadd32333b20bbe0df21612`、`a5b2b7601772478121cdd7a5ca20eda17cc5f9b8`、`c75164209be1c2783012a1d33bd9da96eba19cbf`、`a94f815297e137e748d116dbc6bbc4889dcc3f4e`。
