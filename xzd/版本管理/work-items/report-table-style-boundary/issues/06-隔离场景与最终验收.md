---
tags:
  - report-table
  - work-item
  - verification
status: resolved
date: 2026-08-26
updated: 2026-08-27
---

# 06 — 建立隔离场景并完成最终验收

Parent: [[work-items/report-table-style-boundary/spec]]

**What to build:** 用可持续复查的 Dumi 隔离场景和真实浏览器验收，证明严格边界、双实例 overlay、自动加载及视觉等价同时成立。

**Blocked by:** [[05-移除CSS-Modules双轨并建立边界测试]]

**Status:** resolved

## Scope

- 建立一个持久 Dumi 隔离场景，同屏放置外部 Handsontable、普通 Ant Design 控件和两个报表实例。
- 两个报表实例分别触发 Ant Design 弹层、Handsontable 菜单、筛选弹窗或可达的代表性 portal，并展示独立 overlay 归属。
- 场景支持卸载/重挂任一实例，用于验证宿主与子节点清理且另一实例不受影响。
- 使用真实浏览器分别检查设计态、报表预览、数据预览、外部控件和双实例交互；保存必要截图或在 ticket 评论中记录可复核证据。
- 对比迁移前基线，除外部元素不再受报表样式影响外，不接受视觉、定位、焦点或交互变化。
- 不引入 Playwright、Cypress 或新的浏览器测试依赖。

## Acceptance

- [x] 外部 Handsontable 和普通 Ant Design 控件不被任何报表样式命中或改变。
- [x] 两个报表实例视觉正常，各自弹层只进入自己的 `.udp-report-table--overlay`。
- [x] 卸载一个实例后其宿主完全清理，另一实例及外部控件继续正常工作。
- [x] 设计态、报表预览、数据预览和关键弹层完成真实浏览器操作与截图核对，无非预期差异。
- [x] 从清洁的生成物状态执行 Dumi dev/build 和包 build 均会自动生成所需第三方样式。
- [x] 全部报表 `node:test`、TypeScript 检查、包构建、文档构建和 `git diff --check` 通过。
- [x] ADR、spec、tickets、术语表和版本总览链接完整；最终实施结果追加到本 ticket `Comments`。

## Comments

2026-08-27 最终验收完成：Dumi 场景 `http://localhost:8000/#/excel-table/style-boundary` 在真实浏览器中验证了外部 Handsontable、外部 Ant Design 控件与报表根隔离；两个报表实例分别创建 `udp-report-overlay-1`、`udp-report-overlay-2`，Ant 弹层分别归属对应 overlay。卸载左实例后 `overlay-1` 完全移除、`overlay-2` 保留，外部 HOT/Ant 仍正常；重挂左实例生成新的 `udp-report-overlay-3`。桌面与 390x844 移动视口截图均正常，设计态/报表预览/数据预览场景可加载。

自动化验证：报表包 `node --test tests` 83/83 通过；`npm run check:styles --workspace=@newgrand/udp-report-table` 通过；`npm run build --workspace=@newgrand/udp-report-table` 通过；`npx dumi build` 通过；`git diff --check` 通过（仅 CRLF 提示）。根 TypeScript 仍受仓库既有 TS 4.9.5 与 `@types/d3-dispatch@3.0.7` 的 TS5 语法冲突影响，未扩大范围升级依赖。未修改 `@newgrand/udp-ui` Table，并保留用户已有 `ngproxy.ini`、`preview/plugins/filterRender/range.ts` 变更。
