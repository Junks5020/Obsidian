Status: resolved

# 05 集成边界测试与收口验收

Blocked by: [[issues/01-报表最小兼容API适配]] · [[issues/02-工具栏与前端权限映射]] · [[issues/03-打印数据类型与契约]] · [[issues/04-pdfmake使用示例与组件边界文档]]

相关：[[../spec]] · [[report-table-ADR-0005-第一阶段只交付组件库]]

## Goal

从公共导出、组件边界、服务适配和打印数据消费四个方向完成第一阶段收口，确保未支持能力不会被误报为已兼容。

## Acceptance

- [x] 现有 node:test、TypeScript 检查和包构建通过。
- [x] 公共导出、props、事件和持久化结构无非必要变更。
- [x] 不引入业务路由、后端代码、Supcan/PDF 打印运行时或 `pdfmake` 依赖。
- [x] 运行一次完整验收并在本 ticket `## Comments` 记录结果、命令和遗留风险。

## Comments

### 2026-08-18 实施完成（claimed → resolved）——第一阶段（ticket 01–04）收口验收

在本阶段全部交付后执行一次完整验收：

**命令与结果**

- `node --test packages/@newgrand/udp-report-table/tests/*.test.cjs`：
  **120/120 通过**
  - feature-sync、migration-parity、report-fonts、print-design-payload、boundaries（41）以及新增
    report-table-min-api（18）、report-print-data（13）、report-toolbar-permissions（11）、
    report-print-pdfmake-example（5）。
- 包级类型检查：`npx tsc -p tsconfig.check.json --noEmit` → 本包 `src`/`examples` **0 错误**
  （唯一报错为预先存在的环境噪音 `node_modules/@types/d3-dispatch`，非本阶段引入）。
- 构建：`FATHER_TSCONFIG_NAME=tsconfig.build.json father build` → **成功**（es+lib，306 文件，
  含 `api/reportLimit`、`api/reportPermissionProvider` 与新目录 d.ts；`examples/` 不产出）。

**新增收口边界断言（`udp-report-table-boundaries.test.cjs`）**

- 公共入口运行时默认导出仍仅 `DesignReportTable` + `ReportTable`；phase1 值导出
  （validatePrintData/serializePrintData/createReportTableApi/REPORT_TOOL_BAR_FUNC）为有记录的集合。
- 入口不含 `buildDocumentDefinition` / `buildPdfMakeDocumentDefinition`；es/lib 不含 `examples/`。
- manifest 无 `pdfmake` dependency/peerDependency。
- 纯适配器 `reportTableApi.ts` 仅允许依赖兄弟模块 `./reportLimit`；DesignReportTable 接线含
  `getPermission`。

**边界确认（四方向）**

- 公共导出：只新增 ticket 01/02/03/04 的契约类型与值导出，未改动既有组件默认导出、props、
  事件名与持久化结构（save/load/hiddenAxes/globalSettings 语义未变）。
- 组件边界：仍不自带业务路由（msgfi-frc/msgfi-gc）、后端代码、Supcan/PrintPlugin 运行时、
  pdfmake 依赖；PrintPlugin/Supcan 仅作兼容说明（ticket 04 README）。
- 服务适配：沿用既有 `NG.request` 服务适配层（ADR-0004），未新增接口。
- 打印数据消费：`formdata + Grids` 契约冻结（ticket 03）+ 独立 pdfmake 示例（ticket 04）。

**遗留风险 / 后续**

- `@types/d3-dispatch` 环境噪音与本 worktree 的 father/esbuild 二进制为预先存在的环境问题，
  非本阶段代码；已在本机修复 esbuild linux 二进制后完成构建。
- 代码未提交 git（工作树有大量既有未提交改动）。
- ticket 02 中 `NON_FORMULA`、`WORKSHEET_ADD_AND_DEL` 暂无操作映射（按 ADR-0006 待真实调用样例）。
- 渲染/打印像素级、Supcan、浏览器打印端到端验收不在第一阶段范围（research“测试边界”）。
