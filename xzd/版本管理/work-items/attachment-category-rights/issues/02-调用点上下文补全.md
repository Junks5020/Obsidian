---
tags:
  - ng-design
  - work-item
status: ready-for-agent
date: 2026-08-13
---

# 02 — 调用点上下文补全

Parent: [[work-items/attachment-category-rights/spec]]

**What to build:** 让所有 `normalizeAttachmentPermission` 调用点传入正确的分类上下文，使 ticket 01 的选块逻辑在每个组件与 handler 上生效。

**Blocked by:** [[issues/01-权限归一化按分类选块]]

**Status:** ready-for-agent

- [ ] `formAttachment/index.tsx`：context 增加 `attachType`——审批前 tab 与无 tab 简单形态传单据附件，审批后 tab（`approvedTabKey==='after'`）传审批后附件。
- [ ] `NGLabelAttachment.tsx` / `ImageAttachmentShow.tsx`：context 传单据附件分类。
- [ ] `api/baseApi.tsx`：全部 `getPermission` 调用点补齐 `attachType`（`handleEditOrView`、`handleDelete`、`handleEditCategory`、`handleChangeShareType`、`handleValid`、`handleSave` 等目前未传的）；`handleEditCategory` 同时保留 `typeId`。
- [ ] `api/open.tsx`：`openViewImage` 补单据附件上下文。
- [ ] `NGTableAttachment.tsx`：核对审批后 tab 下 `typeId` 的传递，确认 per-type 块取自 `approvedAttachTypeButtonRights`。
- [ ] 删除 Toolbar 中 workflow tab 不渲染打包下载按钮的特判（改由权限数值驱动）。
- [ ] esbuild 编译检查全部改动文件通过。

## Comments
