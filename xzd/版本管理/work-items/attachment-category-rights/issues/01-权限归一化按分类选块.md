---
tags:
  - ng-design
  - work-item
status: ready-for-agent
date: 2026-08-13
---

# 01 — 权限归一化按分类选块

Parent: [[work-items/attachment-category-rights/spec]]

**What to build:** 重构 `permission.ts` 的 `normalizeAttachmentPermission`：按 `context.attachType`（按类型控制时结合 `typeId`）选择分类权限块，删除兼容字段读取与前端硬编码推导，权限数值原样透传，分类块缺失 fail-closed。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] 按规格映射表实现分类块选择：`attach`/`pendingApprovedAttach` → billAttach*；`approvedAttach` → approvedAttach*；`oriBizAttach` → sourceAttachButtonRights；`workFlowAttach` → workflowAttachButtonRights；按类型控制时单据/审批后取对应 per-type 块的 `[typeId]`。
- [ ] 删除 `unifyButtonRights` / `typeButtonRights` 的全部读取与多层回退链。
- [ ] 删除四条前端推导：审批保护（`approved!==0` 禁增删改）、审批前 tab 禁 `add`、工作流/来源变动权限置 2、工作流 `zipDownload` 置 2。
- [ ] 保留：分类树未选类型 `add/share=2`、`canDeleteTemporaryAttachments`、`canDeleteApprovedAttachment`。
- [ ] `visible` 规则：取激活块的 `visible`；分类树未选类型保留 `hasVisibleType` 兜底；选中类型缺 `visible` 字段按不可见（删除 `\|\| 1` 放行）。
- [ ] 无 `attachType` 的上下文默认取单据附件块（label/image/form 简单形态）。
- [ ] 现有回归用例按新契约修正后，`tsx --test packages/@newgrand/udp-ui/test/attachment-permission.regression.ts` 通过（新增用例在 ticket 03）。

## Comments
