---
tags:
  - ng-design
  - work-item
  - testing
status: ready-for-agent
date: 2026-08-13
---

# 03 — 回归测试扩展

Parent: [[work-items/attachment-category-rights/spec]]

**What to build:** 扩展 `test/attachment-permission.regression.ts`，把规格 Testing Decisions 中的新契约用例全部落地并保持 `tsx --test` 全绿。

**Blocked by:** [[issues/01-权限归一化按分类选块]] · [[issues/02-调用点上下文补全]]

**Status:** ready-for-agent

- [ ] 普通结构（`controlByType: false`）：五个分类各自选中对应权限块。
- [ ] 树状结构（`controlByType: true`）：单据/审批后按 `typeId` 取 per-type 块；未选类型时 `hasVisibleType` 兜底且 `add/share=2`。
- [ ] 响应同时含兼容字段与分类块时忽略兼容字段（unify 全 1、billAttach 变动全 0 → 取 billAttach 的 0）。
- [ ] 0/1/2 透传：后端返回 2 时保持隐藏语义，前端不改写。
- [ ] 分类块缺失 fail-closed；选中类型缺 `visible` 字段按不可见。
- [ ] 保留推导：`canDeleteTemporaryAttachments` 删除提升；`canDeleteApprovedAttachment` 标签附件兜底。
- [ ] 无 `attachType` 上下文默认取单据附件块。
- [ ] `tsx --test packages/@newgrand/udp-ui/test/attachment-permission.regression.ts` 全部通过。

## Comments
