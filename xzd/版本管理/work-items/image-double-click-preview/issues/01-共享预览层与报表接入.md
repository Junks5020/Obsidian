---
tags:
  - report-web
  - work-item
  - image-preview
status: claimed
date: 2026-09-09
updated: 2026-09-10
---

# 01 — 共享预览层与报表接入

Parent: [[work-items/image-double-click-preview/spec]]

**Blocked by:** 无

**Status:** resolved

## Scope

- 建立报表实例归属的图片预览层，处理加载成功门控、关闭和清理。
- 接入图片单元格渲染与浮动图片图层，覆盖设计器和全部桌面端 `PreviewTable` 消费者。
- 用聚焦测试锁定成功、失败、合并和重叠场景，不改保存、接口或导出链路。

## Acceptance

- [x] [[work-items/image-double-click-preview/spec#验收|规格验收条件]]全部满足（自动化覆盖范围内）。
- [ ] 浏览器手工验收：设计器和 ReportTable 的普通/合并图片单元格、浮动图片、重叠命中、Esc/遮罩关闭与双实例实例归属。

## Comments

- 2026-09-09：已领取；开始实现共享、实例归属的预览层与两类图片接入。
- 2026-09-09：实现完成并提交 `5a7307b3`（6.5.2-dev）。
  - 新增 `src/components/report/imagePreview/`：`ImagePreviewHost` 实例归属预览层（等比全屏、遮罩/Esc 关闭、焦点恢复、监听/图层清理、scope 协议），`requestImagePreview`/`openImagePreview` 统一双击入口，`getLoadedImageUrl` 加载门控。
  - 接入图片单元格渲染（设计器与 `PreviewTable` 共用 `HotImageRender`，合并区锚点图片命中）与浮动图片图层（设计/预览双层；双击排除 resize 手柄与锚点角标；设计器管理抽屉保留工具栏入口），覆盖设计器与全部桌面端 `PreviewTable` 消费者（报表/流程/图表/打印预览）。
  - 测试 `tests/image-preview.test.ts`：锁定成功/未加载/加载失败/空地址门控与纯函数不改写，通过。
  - 验证：全量 tsx 回归 29/36——7 个失败为既有环境问题（`@newgrand/udp-core` 模块加载需 `window`），经 stash 基线复测确认与本次改动无关；`tsc --noEmit` 仅既有基线噪音（342 条，均不在改动文件）；`git diff --check` 通过。
  - 待浏览器手工验收（纯 Node 测试无法覆盖的 DOM 交互）：重叠命中规则（顶层浮动图片独占、不向底层透传）、合并图片单元格任一边角双击、Esc/遮罩关闭与焦点恢复、`position: fixed` 弹层在缩放容器中的表现。
- 2026-09-09：同步至 `ng-design:ljx-7.0` 的 `@newgrand/udp-report-table` 工作区（未提交）。
  - 同步 `report-web:6.5.2-dev` 的 `5a7307b3`、`92b5efae` 最终行为：私有 `src/imagePreview/` 受控 antd `Image` 预览层，设计器与 report-mode `ReportTable` 各自挂载实例 scope；图片单元格和设计/预览浮动图片共用双击入口，预览只读取已成功渲染的地址。
  - 按组件包架构适配：弹层容器必须为当前 table context；没有容器时不渲染 `Image`、不回退 `document.body`。浮动图片 `.previewable` 恢复 `pointer-events: auto`，避免图层父级拦截双击。同步测试桩补齐 antd `Image`。
  - 验证通过：`node packages/@newgrand/udp-report-table/tests/image-preview.test.cjs`；`node --test .../calc-mode-reload-loop.test.cjs .../feature-sync.test.cjs .../udp-report-table-boundaries.test.cjs`（54/54）；Prettier、`git diff --check`；包内 `npm run build` 成功并生成 imagePreview ESM/CJS 声明产物。
  - `npx tsc --noEmit -p tsconfig.build.json` 因仓库 TypeScript 4.4 无法解析依赖 `@types/d3-dispatch` 的较新泛型语法而失败，属现有工具链版本不兼容，并非本次改动类型错误。
