---
tags:
  - report-table
  - issue
status: claimed
date: 2026-09-23
updated: 2026-09-23
---

# 当前设计快照与详情手动加载

Status: claimed  
Blocked by: none

## 验收条件

- [x] `DesignReportTableAPI.getValue()` 返回 table/print 当前未保存设计的保存请求结构；对应详情或工作表/HOT 未就绪、处于计算模式时返回 `null`，已就绪的空白设计也可读取。
- [x] `autoLoad={false}` 仅关闭 table 详情自动加载；顶层 `ref.current.load()` 可手动加载，print 详情仍自动加载。
- [x] 手动加载返回 `Promise<boolean>`，继续使用配置的读取执行器、请求过期保护与 loading 生命周期。
- [x] 公共导出、API 文档、完整包测试和构建均完成。

## Comments

- 2026-09-23：领取并完成。新增 `getValue()` 快照构造和 table/print 保存形状类型；已有详情 ID、活动 sheet、HOT 不可用或计算模式时返回 `null`，就绪的空白设计可读取。新增 `autoLoad` 策略和 ref 顶层 `load()`，共用现有详情读取流程及 stale-request 保护。`node --test`：128 项通过；`npm run build --workspace @newgrand/udp-report-table`：ESM、CJS 与类型声明构建通过。追加空白设计用例并通过 6 项定向测试；更新 `docs/excelTable/excel.md`。
