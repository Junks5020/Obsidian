---
id: BUG-20260730-01
type: bug
status: completed
commit: 42d37ff541f1e75f2e1b9f285e35f631a658e364
source_branch: 6.5.2-dev
created: 2026-07-30
target_versions:
  - 6.5.2-dev
  - 6.5.1-dev
tags:
  - version-change
  - bug
  - report-web
---

# BUG-20260730-01：父子树筛选仅包含第一行

## 问题与修复

- 问题现象：父子树报表设计预览中，树列右侧业务列（如 `bill_no`）的筛选弹窗仍只有第一行数据，未包含随父子树展开产生的全部行。
- 根因：真实预览响应的 `globalSettings.treeRows` 为空，完整范围只保存在树列表头的 `filterRange` 中；树列与目标列之间的 `phid_org`、`phid_dept` 表头未启用筛选。旧兼容逻辑要求所有中间表头都具有 `filterRange`，因此无法把树列的完整范围 `1..10` 传递给目标列。
- 修复内容：以连续表头单元格而非连续启用筛选的表头判断同一表格块；保留树列证据、目标列扩展数据校验、横向限制及显式父子树元数据支持。筛选入口统一使用归一化范围和树范围守卫。
- 影响范围：`report-web` 表格设计预览中的单列树、父子树筛选范围，以及包含未启用筛选中间列的树报表。

## Git 信息

- Commit：`42d37ff541f1e75f2e1b9f285e35f631a658e364`
- 分支：`6.5.2-dev`
- 合并方式：来源分支直接提交；通过 cherry-pick 同步至 `6.5.1-dev`，目标提交为 `9b55ee69918d05d365bb448d740818d4262565fe`。

## 版本同步

| 版本名称 | 是否需要同步 | 当前状态 | 合入 Commit | 验证结果 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 6.5.2-dev | 是 | 已同步 | `42d37ff541f1e75f2e1b9f285e35f631a658e364` | 真实页面、自动化、格式及构建通过 | 来源提交 |
| 6.5.1-dev | 是 | 已同步 | `9b55ee69918d05d365bb448d740818d4262565fe` | 自动化、Prettier 2.8.8、补丁检查及生产构建通过 | cherry-pick；保持目标分支已删除的 `references/` 文档不恢复 |

## 验证

- 真实页面：报表 `1240000000000012` 的 `bill_no` 筛选范围由 `1..1` 修复为 `1..10`，可见全部 10 个值。
- 自动化：父子树筛选范围、树折叠、Schema 默认层级、首次渲染、预览可见性、行列可见性共 6 组测试通过。
- 格式与补丁：来源分支 Prettier 3.8.4、目标分支 Prettier 2.8.8 检查通过；`git diff --check` 通过。
- 构建：`6.5.2-dev` Webpack 构建成功（51.95s）；`6.5.1-dev` Webpack 构建成功（57.41s）。
