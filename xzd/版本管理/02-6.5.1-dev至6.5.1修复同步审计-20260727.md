---
tags:
  - version-management
  - sync-audit
  - report-web
---

# 6.5.1-dev 至 6.5.1 修复同步审计

## 审计范围

- 仓库：`C:\Users\jinxu\workspace\xzd\report-web`
- 来源分支：`6.5.1-dev`
- 目标分支：`6.5.1`
- 作者筛选：仅 `liujinxu <liujinxu@newgrand.com>` 的来源提交
- 时间范围：2026-06-29 至 2026-07-25
- 识别方式：先使用 `git cherry` 核对补丁等价，再逐文件检查来源提交的有效新增代码行是否存在于目标分支。

## 结果

| 结论 | 数量 | 含义 |
| --- | --- | --- |
| ✅ 已同步 | 21 | 存在严格补丁等价提交，或有效新增代码在目标分支完整匹配。 |
| ⚠️ 验证中 | 5 | 目标分支仅部分匹配来源提交代码。 |
| ❌ 待同步 | 0 | 未发现任何代码证据。 |

## 待处理

- `BUG-20260630-001`：数据集过滤数据不更新，代码层匹配 `31/46` 条有效新增行。
- `BUG-20260706-003`：预览与 PDF 居中样式，代码层匹配 `1/2` 条有效新增行。
- `BUG-20260707-001`：异常行高，代码层匹配 `4/5` 条有效新增行。
- `BUG-20260715-001`：树状预览展开折叠，代码层匹配 `316/325` 条有效新增行。
- `BUG-20260716-001`：树形报表默认展示层级，代码层匹配 `102/125` 条有效新增行。

完整状态见 [[01-版本覆盖矩阵|版本覆盖矩阵]]。
