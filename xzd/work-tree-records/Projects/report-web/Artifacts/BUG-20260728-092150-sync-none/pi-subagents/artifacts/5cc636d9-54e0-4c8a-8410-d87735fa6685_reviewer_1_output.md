## Review

- **Correct：实现与真实渲染机制匹配。**  
  `src/components/report/preview/plugins/treeRender/utils.ts:303-305` 先同步隐藏行、写入 `children`/`expanded`，并保存树状态；之后才在 `:311` 调用 `hot.render()`。生产调用路径在 `src/components/report/preview/tableSheets/utils.ts:184-217` 中确实处于 `suspendRender()` / `resumeRender()` 之间。Handsontable 的真实实现会在暂停期间将 `renderCall` 和 `forceFullRender` 置位而不立即绘制，恢复时再执行渲染（`node_modules/handsontable/core.js:1791-1800,1813-1821`）。因此首次 viewport 重绘时树元数据已经就绪，修复逻辑成立。

- **Correct：row=0 回归场景被明确覆盖。**  
  `tests/tree-preview-collapse.test.ts:108-123` 首次初始化后检查 row 0、col 1（mock 坐标键 `0:1`）的首次 render snapshot，要求其等于 `{ children: [5], expanded: false }`。这既验证了 row=0 没有被 truthy/falsy 判断漏掉，也验证了 `children` 与 `expanded` 在 `render()` 请求发生前已经写入。删除 `hot.render()` 或将其移到 `syncTreeCellMeta()` 之前，该测试都会失败。

- **Correct：测试计数合理。**  
  `tests/tree-preview-collapse.test.ts:118-140` 的预期总数 5 对应：
  1 次初始化 render 请求，加上 4 次成功 toggle；`toggleTreeNode(..., 7, 0)` 返回 false，不应增加次数。中间计数 2 和最终计数 5 均一致，没有明显 off-by-one 或为了迁就实现而重复计数的问题。

- **Correct：schema 测试的改动最小。**  
  `tests/tree-preview-schema-level.test.ts:43-50` 只为既有 mock 增加 `render()`，保持该测试仍专注于 schema showLevel；没有加入与其职责无关的计数断言。

- **Blocker：无。**  
  未发现会阻止合入的正确性或测试充分性问题。

- **Note（低风险，测试可靠性）：当前测试验证的是同步 render 请求顺序，而不是真实 suspend/resume 生命周期。**  
  `tests/tree-preview-collapse.test.ts:22-54` 的 mock 没有 `suspendRender()`、`resumeRender()` 或 pending-render 状态，因此 `:122` 的描述“before the initial render resumes”比实际测试能力稍强。当前 snapshot 对本次回归已经具备有效防护，但更可靠的后续方式是让 mock 模拟暂停状态：暂停后调用 `initializeTreeRows()`，确认只登记 render 请求；恢复时捕获首次实际 viewport snapshot。更高强度的选择是增加一个 Handsontable/jsdom 集成测试，覆盖 `handleLoadGlobalSettings()` 的完整顺序。该缺口为残余风险，不构成 blocker。

- **Note（简洁性）：当前生产实现已经接近最小。**  
  实质改动只有 `src/components/report/preview/plugins/treeRender/utils.ts:311` 一次 `hot.render()`。把它移到 `tableSheets/utils.ts` 的 `resumeRender()` 前虽然能减少工具函数对 preview loader 的注释耦合，但不会减少逻辑量，并会削弱 `initializeTreeRows()` 自身保证可视状态刷新的能力。测试 snapshot 可以缩成只保存目标 row 0 元数据，但当前通用快照约十行，复杂度仍属合理，无必要为简洁性重写。

- **Note（验证环境）：** 两个目标回归测试均通过，diff whitespace 检查通过。全量 TypeScript 检查因既有第三方声明冲突、缺失依赖以及仓库其他文件错误而失败，输出中未见本次三个改动文件的新增类型错误。ESLint 对 `src` 文件没有报告问题，但两个 `tests/*.ts` 不在当前 `tsconfig.json` include 中，导致聚合 lint 命令以配置解析错误退出。